{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{displayName}} — {{ecosystem}} ecosystem navigation",
  "author": "{{authorName}}",
  "license": "MIT",
  "homepage": "https://github.com/{{authorName}}/{{projectName}}",
  "type": "module",
  "scripts": {
    "dev": "npx serve src",
    "build": "node build/build.js",
    "preview": "npx serve docs -l 3000"
  }
}
